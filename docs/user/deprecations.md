---
title: Deprecations and compatibility
description: What KiCI has deprecated, what to use instead, when each deprecated surface is removed, and which error messages changed or values are now rejected.
---

KiCI keeps its published surfaces working across `0.x` releases. When a
surface changes, the old form is **deprecated** — it keeps working, marked
deprecated, alongside the new form — rather than removed outright. Deprecated
surfaces are removed only at a **major version bump**; while KiCI is on the
`0.x` line, that first sweep is **v1.0.0**.

This page is the running list of everything currently deprecated on a
customer- or operator-facing surface: the SDK, the `kici` and `kici-admin`
CLIs, `.kici/` configuration, and the customer-facing side of the wire
protocol, lock file, and storage layout.

## How to read this table

| Column            | Meaning                                                                                |
| ----------------- | -------------------------------------------------------------------------------------- |
| **Surface**       | Where the deprecation applies (SDK export, CLI flag, config field, protocol field, …). |
| **Deprecated**    | The old form that still works but is marked deprecated.                                |
| **Replacement**   | What to migrate to.                                                                    |
| **Deprecated in** | The release that introduced the deprecation.                                           |
| **Removal**       | The release the old form is removed in (defaults to `v1.0.0` on the `0.x` line).       |

## Deprecated surfaces

| Surface                                                            | Deprecated                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Replacement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Deprecated in | Removal                                   |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ----------------------------------------- |
| Wire protocol (provenance)                                         | Platform-rooted identity mint (`oidc.mint.*` RPC) — the orchestrator relays each build's identity-token mint to the hosted Platform                                                                                                                                                                                                                                                                                                                                            | Orchestrator-owned signing: the orchestrator holds its own ES256 key, mints + signs identity tokens locally, and publishes its own OIDC discovery + JWKS (configure `KICI_ORCHESTRATOR_PROVENANCE_ISSUER`). Existing Platform-signed bundles keep verifying forever (the Platform JWKS stays published).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | v0.2.0        | v1.0.0                                    |
| Dashboard-write policy value                                       | Boolean per-operation policy (`true` = enabled, `false` = disabled) in the stored policy and on the `kici-admin org-settings dashboard-writes set --op <name>=<true\|false>` flag                                                                                                                                                                                                                                                                                              | Tri-state posture `permissive \| encrypted \| disabled`. Booleans are still accepted everywhere (`true` → `permissive`, `false` → `disabled`) and stored rows are auto-migrated; `--op <name>=<permissive\|encrypted\|disabled>` is the new form. No removal until v1.0.0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | v0.2.0        | v1.0.0                                    |
| Wire protocol (artifacts)                                          | `artifacts.upload.complete` fields `storageKey` (the storage key echoed back by the agent) and `sizeBytes` (the artifact size declared by the agent)                                                                                                                                                                                                                                                                                                                           | Nothing to change on your side: the orchestrator derives the storage key from the run and artifact name it resolved server-side, and records the real object size it reads back from storage. Both fields are still accepted, and the agent still sends them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | v0.2.0        | v1.0.0                                    |
| Object-storage layout (user cache)                                 | `cache/<orgId>/<repoId>/<scope>/<key>.tar.gz` — the cache key was the object-name segment on its own, so on a case-insensitive store two keys differing only by case resolved to one object                                                                                                                                                                                                                                                                                    | `cache/<orgId>/<repoId>/<scope>/<key>-<discriminator>.tar.gz`, where the discriminator is a hash of the exact cache key. Nothing changes on your side: entries in the old form are still read back on an exact-key restore for one cache-TTL window (default 7 days), and `restoreKeys` prefix matching is unaffected because the discriminator is a suffix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | v0.2.0        | v1.0.0                                    |
| HTTP API (webhook test ping)                                       | `success` in the `POST /orgs/:customerId/webhook-endpoints/:endpointId/test` response body                                                                                                                                                                                                                                                                                                                                                                                     | `delivered` — the same boolean under a name that says what it means. Both fields are sent with the same value. A test ping for an endpoint that is not your organization's now answers **404** instead of `200 {"success": false}`, so check the HTTP status too.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | v0.2.0        | v1.0.0                                    |
| Held runs (`holdType` value)                                       | `approval` for a reviewer hold and `wait_timer` for a workflow-install wait hold, on the held-runs read surface and in the `held_runs.hold_type` column                                                                                                                                                                                                                                                                                                                        | `reviewer` and `timer` — the same four-value hold-type vocabulary the protection gates emit (`reviewer`, `timer`, `concurrency`, `security`). Nothing to change on your side: your orchestrator backfills existing rows on upgrade and keeps accepting both spellings on read, so a held run written before the upgrade still resolves and displays correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | v0.2.0        | v1.0.0                                    |
| SDK `needs[].when` / lock-file `runOn`                             | `'on-failure'` expanding to `failed, timed_out_stale` only, so a downstream error handler did **not** run when its upstream was dropped by determinism drift — even though such a run is already reported as failed                                                                                                                                                                                                                                                            | `'on-failure'` now expands to `failed, timed_out_stale, drift_dropped`. The change is additive: an error handler starts firing in a case it should always have covered, and none stops firing. Pass a raw status set (`when: ['failed', 'timed_out_stale']`) to keep the narrower behaviour. **When it takes effect:** a static job's edge is expanded at compile time, so an already-deployed lock file keeps its frozen set until the workflow is recompiled; a `dynamicJob`'s edges are expanded on the agent at run time, so those pick up the widened set as soon as the agent is upgraded, with no recompile.                                                                                                                                                                                                                                                                                                                | v0.2.0        | n/a — a semantic widening, not a removal  |
| Wire protocol / job status vocabulary                              | The terminal job-status set having no member for "the job's `runsOn` matched no agent" — such a job was never tracked at all, so a run could report **success** while a job it declared never ran                                                                                                                                                                                                                                                                              | A new `unroutable` terminal job status. A job that stays queued for the whole window with no agent able to run it settles `unroutable`, its check-run conclusion is `failure`, the job's error message names the unsatisfied `runsOn`, and the run fails. The change is additive: `unroutable` joins `failed, timed_out_stale, drift_dropped` in the `'on-failure'` expansion, so an error handler starts firing in a case it should always have covered, and none stops firing. **Deploy the Platform before the orchestrator** — an older Platform rejects the unrecognised status value.                                                                                                                                                                                                                                                                                                                                        | v0.2.0        | n/a — an additive status, not a removal   |
| `kici-admin secret scopes`                                         | Listing only the PG backend, with scopes printed unqualified                                                                                                                                                                                                                                                                                                                                                                                                                   | `kici-admin secret scopes <orgId> --all-backends` (and `GET /api/v1/admin/secrets/scopes?allBackends=true`), which aggregates every registered backend and prints scopes in qualified `<backend>:<path>` form. The bare pg-only output is unchanged until **v1.0.0**, when `--all-backends` becomes the default; pass the flag now if your scripts should see every backend.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | v0.2.0        | v1.0.0                                    |
| Global workflows (elevated access)                                 | The **Elevated access** list (`global_workflow_elevated_repos`) — the dashboard card, the `elevatedRepos` field on the global-workflow settings API and wire messages, and the `kici-admin org-settings global-workflows elevate-add` / `elevate-remove` mutators. It reads as a grant of the triggering repo's secrets to a listed workflow repo, and nothing enforces it.                                                                                                    | Nothing — there is no replacement, because there was never a grant. An organization-wide workflow's job is dispatched with **no secret material at all**, neither the source repo's nor its own, so no entry in this list ever made a secret readable. Clear the list so it does not imply a permission that is not in force. To run jobs that need credentials on a source repo's event, put them in a per-repository workflow in the repo that owns the secrets. The setting keeps storing and echoing values until removal so an existing value stays visible and clearable.                                                                                                                                                                                                                                                                                                                                                    | v0.5.0        | v1.0.0                                    |
| Global workflows (agent requirement)                               | Agents older than **v0.5.0** running an organization's global workflows. Such an agent has no branch for the pre-run evaluation job the orchestrator now dispatches, so the job falls through to the standard executor and the round never reports a verdict.                                                                                                                                                                                                                  | Agents at **v0.5.0 or newer**. **Upgrade every agent that carries the `kici:role:init-runner` label before you upgrade the orchestrator** — the reverse order suppresses every global workflow that declares a `filter` **or** contains a `dynamicJob` generator, including its **static** jobs, which ran before the upgrade and stop running after it with no change to the workflow. Each affected push instead gets a failing `KiCI: Organization workflow evaluation` check. Your orchestrator detects the all-too-old case and says so in that check rather than timing out; a fleet only **partly** upgraded is not detected, because the round may still be routed to an old agent. Non-global (per-repository) workflows are unaffected either way.                                                                                                                                                                       | v0.5.0        | n/a — a deploy-ordering requirement       |
| Lock-file `LockInlineValue` (`_type: 'inline'`)                    | Serialized inline dynamic-field expressions — the compiler serialized a pure `env` / `context` / `concurrencyGroup` function into the lock as `{ _type: 'inline', expression: '(event) => …' }` for the orchestrator to evaluate at dispatch                                                                                                                                                                                                                                   | Agent-resolved dynamic fields: dynamic `env` / `context` / `concurrencyGroup` functions are resolved on the eval agent's init-runner, and the orchestrator no longer evaluates any workflow code in-process. **No author change required — the SDK API is unchanged.** The compiler no longer emits inline values; a reader still recognizes the shape in an already-deployed lock and defers that field to the init round, so an old lock keeps running until it is recompiled.                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v0.4.0        | v1.0.0                                    |
| Plan orchestrator limit (meaning)                                  | The orchestrator plan limit counting only orchestrators that hold a Platform WebSocket connection, so worker orchestrators in a coordinator/worker cluster did not count toward it.                                                                                                                                                                                                                                                                                            | The limit now counts every orchestrator **instance** — coordinators and workers alike — so a cluster consumes one unit per running orchestrator. Tier values are unchanged (Free 2 / Pro 5 / Team 15 / Business 50); only what they measure got broader. An organization over its limit is notified, and after a 7-day grace window its newest workers are drained down to the limit. Nothing to change on your side unless you were running uncounted workers on the Free tier.                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v0.6.0        | n/a — a semantic change, not a removal    |
| Orchestrator env `KICI_TEST_MODE`                                  | Master switch for the orchestrator's synthetic fault-injection knobs. It was an internal E2E aid, never a supported feature, and was only ever documented as test-only.                                                                                                                                                                                                                                                                                                        | None. Fault injection now lives entirely in a build-time test double that never ships in a customer image, so the shipped orchestrator reads no such env var. Remove it from any orchestrator environment; it has no effect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | v0.6.0        | v0.6.0 — test-only internal knob, removed |
| Orchestrator env `KICI_TEST_EVENT_FAIL_FIRST_N`                    | JSON map that made the EventRouter throw a synthetic dispatch error for the first N attempts of a named event. Internal E2E aid, never a supported feature.                                                                                                                                                                                                                                                                                                                    | None. The event-dispatch fault map is supplied only by the build-time test double, which never ships in a customer image. Remove it from any orchestrator environment; it has no effect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | v0.6.0        | v0.6.0 — test-only internal knob, removed |
| Orchestrator env `KICI_TEST_MINT_DEFER_AUDIENCE`                   | Forced the initial provenance mint to defer for a marker OIDC audience. Internal E2E aid, never a supported feature.                                                                                                                                                                                                                                                                                                                                                           | None. The mint-defer fault is supplied only by the build-time test double, which never ships in a customer image. Remove it from any orchestrator environment; it has no effect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v0.6.0        | v0.6.0 — test-only internal knob, removed |
| Orchestrator env `KICI_TEST_MINT_REJECT_AUDIENCE`                  | Forced the retrier's re-mint to terminally reject a marker OIDC audience. Internal E2E aid, never a supported feature.                                                                                                                                                                                                                                                                                                                                                         | None. The mint-reject fault is supplied only by the build-time test double, which never ships in a customer image. Remove it from any orchestrator environment; it has no effect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | v0.6.0        | v0.6.0 — test-only internal knob, removed |
| Orchestrator env `KICI_TEST_RERUN_DELAY_MS`                        | Made `handleRerunRequest` sleep before invoking the rerun, so an HA test could force a relay failover. Internal E2E aid, never a supported feature.                                                                                                                                                                                                                                                                                                                            | None. The rerun-delay hook is supplied only by the build-time test double, which never ships in a customer image. Remove it from any orchestrator environment; it has no effect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v0.6.0        | v0.6.0 — test-only internal knob, removed |
| Orchestrator env `KICI_TEST_OMIT_DASHBOARD_REQUEST_TYPES`          | Dropped named `dashboard.*` request types from the advertised capability manifest, to reproduce an older orchestrator. Internal E2E aid, never a supported feature.                                                                                                                                                                                                                                                                                                            | None. The capability transform is supplied only by the build-time test double, which never ships in a customer image. Remove it from any orchestrator environment; it has no effect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | v0.6.0        | v0.6.0 — test-only internal knob, removed |
| Orchestrator env `KICI_SKIP_S3_SENTINEL_VALIDATION`                | Runtime escape hatch that skipped the S3 split-brain sentinel validation on cluster-identity bootstrap. Internal E2E aid, never a supported feature.                                                                                                                                                                                                                                                                                                                           | None. The split-brain guard can no longer be disabled at runtime — the shipped orchestrator always validates the sentinel. Only the build-time test double, which never ships in a customer image, can skip it. Remove this var from any orchestrator environment; it has no effect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | v0.6.0        | v0.6.0 — test-only internal knob, removed |
| `@kici-dev/sdk` `buildAgentCloudInit`                              | The agent-token credentials form (`CloudInitCredentials` — `{ agentToken, agentId, orchestratorUrl, labels }`), which bakes the ephemeral agent token into the rendered cloud-init.                                                                                                                                                                                                                                                                                            | The claim-code form (`ClaimCodeCredentials` — `{ claimCode, agentId, orchestratorUrl, labels }`): pass the single-use claim code from the scale-up event and the provisioned agent self-claims its token in-instance, so the token never transits the provisioning channel. Both credential shapes are still accepted, so the token form keeps working until removal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | v0.6.0        | v1.0.0                                    |
| Org trust policy (non-fork arms)                                   | `unknownContributorPolicy` and `workflowChangePolicy` — on the `trust_policy.update` wire message, on the orchestrator admin trust-policy route, and as the `kici-admin trust-policy set --unknown-contributor-policy` / `--workflow-change-policy` inputs. Still accepted, still stored, and still echoed back so an older orchestrator or CLI keeps seeing the value it sent, but the gate reads neither, so setting one changes no dispatch outcome.                        | `kici-admin trust-policy set --fork-policy ignore\|hold\|allow` alone. The trust-policy gate collapsed to the three-state fork switch: a `.kici/` modification is surfaced on its own informational check instead of feeding a policy arm, and contributor identity no longer gates dispatch. The matching `held_runs.reason` values `workflow_modification` and `unknown_contributor` are no longer raised either — rows written by earlier builds keep them, and `fork_pr` is the only reason the org policy now produces.                                                                                                                                                                                                                                                                                                                                                                                                       | v0.6.0        | v1.0.0                                    |
| Admin HTTP API (trust policy `enforcement`)                        | `enforcement` (`policy` \| `legacy`) in the orchestrator trust-policy response body. It is now always `policy`: a policy is resolved for every input, so the values alongside it are always the ones being applied and there is no state left in which they are absent.                                                                                                                                                                                                        | Nothing — read the policy fields directly. The field stays emitted only so an older `kici-admin` binary, which reads it to decide whether to render those fields, keeps working.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v0.6.0        | v1.0.0                                    |
| Orchestrator contributor-cache TTL                                 | `kici-admin cluster-settings set --contributor-cache-ttl-ms`, its `contributorCacheTtlMs` admin-API field, and the `KICI_CONTRIBUTOR_CACHE_TTL_MS` orchestrator environment variable. All three stay settable and `cluster-settings show` still reports the stored value, but no behavior reads it.                                                                                                                                                                            | Nothing — drop it from your configuration. Trust is resolved from the ref a pull request pushes to, so the orchestrator makes no contributor-permission lookup and has no cache to size.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | v0.6.0        | v1.0.0                                    |
| Org trust policy (`forkPolicy` value `reject`)                     | `reject` on the `trust_policy.update` wire message, on the orchestrator admin trust-policy route, and as `kici-admin trust-policy set --fork-policy reject`. Still accepted and still stored as given; `kici-admin` prints a warning when you set it.                                                                                                                                                                                                                          | `ignore`, which is what `reject` already resolves to: the fork event is dropped before dispatch, so there is no run row and no check run. `reject` keeps working until removal, so a stored value needs no rewrite. **Migration:** set `--fork-policy ignore` (or pick **Ignore** in **Settings > CI trust**) the next time you touch the policy. One difference while an orchestrator predates protocol version 2: the Platform sends it `reject` in place of `ignore`, and that orchestrator posts a failed run rather than dropping the event silently.                                                                                                                                                                                                                                                                                                                                                                         | v0.6.0        | v1.0.0                                    |
| Trust tier value `known`                                           | `known` on `execution_runs.trust_tier`, on the run read surfaces that echo it (`kici runs show`, the dashboard run detail, the agent run-result message), and as a `TrustTier` member. Nothing produces it: trust is resolved from the git ref, which yields `trusted` for a ref in the base repository and `unknown` for a ref from a fork.                                                                                                                                   | Nothing to change. The value stays readable so a run recorded by an earlier build still renders, and an internally-triggered run may still inherit it from such a row. It behaves as an untrusted tier everywhere except the context `minimumTrust` gate, which passes it — see the row below. At v1.0.0 trust becomes a two-value vocabulary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | v0.6.0        | v1.0.0                                    |
| Context `minimumTrust` value `known`                               | `known` as a context's `minimumTrust` floor, on `kici-admin context set-policy`, the orchestrator admin context routes, and the dashboard Protection tab. Still accepted and still stored.                                                                                                                                                                                                                                                                                     | `trusted`. Both values now block the same thing — a run whose ref came from a fork — because trust is a two-value, ref-based judgement, so there is no floor left to rank. The declared value still decides the wording of the hold reason. **Migration:** set `minimumTrust: 'trusted'` on every context that declares `known`. **One relaxation to check:** a context set to `trusted` no longer holds a run whose tier resolved `known`. That tier is only reachable through a stored run row written by an earlier build, inherited by an internally-triggered run, so a `kiciEvent()` subscriber of such a run now passes a `trusted` floor that used to hold it.                                                                                                                                                                                                                                                             | v0.6.0        | v1.0.0                                    |
| Per-member CI trust override                                       | `PUT /orgs/:customerId/members/:userId/ci-trust`, the `ci_trust_override` it writes, and the Members-tab editor that called it. The route is still accepted, the stored override is still applied, and it still supersedes the level the member's roles grant. Each call logs a deprecation warning. The dashboard no longer offers the editor; the Members tab shows the effective level, names a stored override on the badge, and offers a `ci_trust:admin` member a clear. | Grant the level through a **role** the member holds, directly or through a team. `ci_trust` is approval authority — it decides who may release a security hold — so a role is the right place to express it and it is visible to anyone reading the role. **Migration:** for each member carrying an override, create or assign a role whose `ci_trust` level is the one you want, then `PUT … /ci-trust` that same level so the override and the roles agree. Or clear the override outright — the Members tab offers this to a `ci_trust:admin` member, and `PUT … /ci-trust` with `level: "none"` does the same, storing NULL rather than the literal `none`. An override that instead matches the role-derived level changes no outcome, so removal at v1.0.0 is a no-op either way. Only a **user** actor resolves an override at all: an organization API key uses its own permission matrix and never inherits its owner's. | v0.6.0        | v1.0.0                                    |
| Provider interface `ContributorResolver`                           | The `ContributorResolver` interface and its `ContributorInfo` / `ContributorPermission` types, exported from `@kici-dev/engine`. A provider bundle no longer carries one and the webhook pipeline never calls one, so an implementation you supply is never invoked.                                                                                                                                                                                                           | Nothing — remove the implementation. Trust is resolved from the git ref the event carries, with no provider permission call, so there is nothing left for a resolver to answer. The types stay exported so an out-of-tree provider keeps compiling until removal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | v0.6.0        | v1.0.0                                    |
| Provider interface `WebhookNormalizer.getAccessCacheInvalidations` | The optional `getAccessCacheInvalidations` method and its `AccessCacheInvalidation` type, exported from `@kici-dev/engine`. A normalizer that implements it is still accepted, but the orchestrator holds no contributor-permission cache, so nothing calls it and the entries it returns are discarded.                                                                                                                                                                       | Nothing — drop the method from your normalizer. It described which cached provider-permission entries a webhook event invalidated, and there is no such cache. The method stays optional and the type stays exported until removal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | v0.6.0        | v1.0.0                                    |
| Object-storage layout (dependency cache)                           | `deps/{platform}-{arch}/{lockfileHash}.tar.gz` for the dependency tarball, with its integrity hash in a companion `deps/{platform}-{arch}/{lockfileHash}.hash`. Both keys derive from the lock file alone, so two builds that share a lock file and platform share both — and a build that finished second could leave its hash beside the other build's tarball. An agent then rejected the download with `Dep tarball hash mismatch` on every retry, and the job failed.     | `deps/{platform}-{arch}/{depsHash}.tar.gz` — the tarball is addressed by its own content hash — with `deps/{platform}-{arch}/{lockfileHash}.hash` holding that hash as a pointer. The tarball is immutable, so a pointer and the bytes it names can never disagree. **Nothing to change on your side**, and no action is needed: entries in the old form are not read back, so the first build after the upgrade repopulates the cache for each lock file. Old objects are never referenced again and are removed by the normal cache TTL (`KICI_CACHE_TTL_DAYS`, default 30 days). An entry with no pointer is now a cache miss rather than a download served without an integrity check.                                                                                                                                                                                                                                         | v0.6.0        | v0.6.0 — old keys are orphaned, not read  |
| Wire protocol (scaler capacity)                                    | `scalerCapacity[].mandatoryLabels` on the peer heartbeat — one taint gate for the whole scaler, formed by unioning the gates of every label set it declares. A scaler that mixes platforms therefore advertised a gate no single label set could satisfy: a pool declaring `[linux, gpu]` alongside `[macos, xcode]` gated its linux set on `macos`, and no sensible job requests both, so that label set accepted nothing.                                                    | `scalerCapacity[].labelSetMandatoryLabels` — one gate per label set, index-aligned with `labelSets`. **Nothing to change on your side.** Both fields are sent: an orchestrator that understands the new one prefers it, and one that does not keeps reading the union exactly as before. The new field is omitted rather than empty when a peer does not send it, so "no per-set gate advertised" stays distinguishable from "every label set is ungated", and a receiver falls back to the union whenever the array is absent or its length does not match `labelSets`. Upgrade order does not matter.                                                                                                                                                                                                                                                                                                                            | v0.6.0        | v1.0.0                                    |

When a surface is deprecated, it is added here in the same change that
introduces the replacement, and it appears in the changelog for that release.

## Changed error messages and rejected values

Some changes cannot go through the table above, because there is no old form to
keep working alongside the new one. Error **text** is one: there is no way to
emit an old sentence and a new one at once. A value that is now **refused** is
the other — where accepting it was itself the defect, continuing to accept it
for a deprecation window would keep shipping the bug. Both take effect in the
release that ships them. The changes worth naming:

| Surface                                                  | What changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Changed in |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| SDK (`ctx.artifacts`)                                    | An artifact name that breaks the [name contract](./sdk/artifacts.md) now fails `upload()` / `download()` with a readable sentence — `artifact "bad/name": invalid artifact name: artifact name may only contain letters, digits, ".", "_", and "-"` — instead of a raw dump of the schema-validation issues. Catching the failure, testing it with `instanceof Error`, and reading `.message` are unaffected; matching the schema library's own error class, or reading its `issues` array, no longer works.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | v0.2.0     |
| Admin HTTP API (secrets)                                 | A routing-key-scoped admin token (`kici-admin token create --routing-key <key>`) is now refused with **403** on every admin secret route. It previously reached the secret store for the one scope whose name exactly equalled the token's routing key — listing that scope's keys, deleting the scope, and deleting a secret in it. There is no dual-accept window, because the same release validates scope names segment by segment against `[A-Za-z0-9_.-]`, which admits no `:`, while every routing key is `<provider>:<id>` — so the single scope the grant covered is no longer a name that can be created. The grant was a namespace confusion rather than a usable slice of the secret store: a routing key identifies an inbound source, whereas a secret scope is a path in a backend namespace where `:` is reserved to qualify the backend. Use an unscoped admin token for secret management; routing-key scoping is unchanged everywhere else (sources, runs, events, registrations, the event log).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | v0.2.0     |
| `kici-admin context` (`--hold-expiry`)                   | A hold expiry of `0` is now rejected with `context: holdExpirySeconds must be >= 1` on `context create`, `context set-policy` and `context create-template`, with HTTP 400 on the matching orchestrator admin routes, and on the dashboard Protection tab. A stored `0` placed each hold's deadline at the instant it was created, so the hold expired before a reviewer could act and the job it gated was cancelled — accepting the value was the defect, so there is no dual-accept window. Any context already holding `0` is backfilled to no explicit expiry on upgrade, which resolves to the documented one-hour default. To clear an existing hold expiry, pass an empty value to `context set-policy` (`--hold-expiry ""`); that previously parsed to `0` and now sends an explicit clear. On `context create` and `context create-template` there is nothing to clear — omit the flag, since an empty value there is still read as `0` and rejected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | v0.2.0     |
| Held-run approve / reject (security holds)               | Each hold type now takes exactly the permission that matches it, on the held-run approve **and** reject routes — the dashboard, the `kici` CLI, and any direct caller of those routes alike. The per-type rules below describe a hold on a run **inside** the caller's repository patterns: a repository-scoped caller acting on a hold outside its patterns is refused first, with a **403** that names neither the hold's type nor a required permission, so that refusal reads the same whatever kind of hold it was. A **security** hold requires `ci_trust:write`; `contexts:write` alone is refused with **403** and `requiredPermission: "ci_trust:write"`. Symmetrically, `reviewer` and `concurrency` holds require `contexts:write`, so a principal holding only `ci_trust:write` no longer releases them and is refused with `requiredPermission: "contexts:write"`. (A `timer` hold requires `contexts:admin` — see the wait-timer row below.) Both routes previously accepted **either** permission for **every** hold type, and the distinction existed only in the dashboard's button gating — so anything speaking to the API directly released security holds with the broader grant. Accepting that was the defect, and a dual-accept window would keep the privilege separation unenforced, so there is no such window. These routes can also now answer **502** when the hold's type cannot be resolved: the check fails closed rather than guessing. **Migration:** grant `ci_trust:write` to the roles that release security holds, `contexts:write` to any role that releases reviewer or concurrency holds, and `contexts:admin` to any role that releases wait-timer holds. A stored per-member CI trust override still supersedes the role-derived `ci_trust` level on every surface, dashboard included, but it does not by itself let a member reach these routes, and the override is deprecated — grant the level through a role. An org API key uses its own permission matrix and does not inherit its owner's override. | v0.2.0     |
| Held-run approve / reject (wait-timer holds)             | Approving, rejecting, or skipping a **`timer`** hold now requires `contexts:admin`. It previously accepted `contexts:write` over the API and the `kici` CLI, while the dashboard already required `contexts:admin` to skip a wait and the documentation promised it — so the stricter rule existed only client-side and anything speaking to the API directly skipped a wait with the broader grant. The server now enforces `contexts:admin` on every surface; a caller holding only `contexts:write` is refused with **403** and `requiredPermission: "contexts:admin"`. Accepting the weaker grant was the defect, so there is no dual-accept window. The legacy persisted spelling `wait_timer` normalizes to `timer` and takes the same rule. **Migration:** grant `contexts:admin` to the roles that skip wait timers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | v0.2.0     |
| Developer MCP (`approve_run` / `reject_run`)             | Both tools now require the hold's own permission **in addition to** `runs:write`. They previously checked only `runs:write`, so an agent credential released a **security** hold without `ci_trust:write` — a wider grant than the one the held-run routes had already been tightened to require. Each tool now applies the same policy the Platform's approve/reject routes apply: `ci_trust:write` for a `security` hold, `contexts:admin` for a `timer` hold, `contexts:write` otherwise; `runs:write` is still required as well. A call that fails the narrower check returns `Insufficient permission: <resource>.<level> needed to act on this hold` and never relays the decision. Accepting `runs:write` alone was the defect, so there is no dual-accept window. **Migration:** grant an agent credential the permission matching the hold classes it is meant to release.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | v0.2.0     |
| Secret key names                                         | A secret key is now validated against `[A-Za-z0-9._-]` (maximum 256 characters) on every write plane — `kici-admin secret set`, `PUT /api/v1/admin/secrets/:orgId/:scope/:key`, the dashboard's add-secret form, and the `.kici/secrets.yaml` / `.kici/.secrets` seeding `kici run <event> --local` performs (which stops at the offending key, leaving the secrets already seeded in place — rename it and re-run) — and a key outside that set is refused with `Secret key may only contain letters, digits, and _ . - characters` (HTTP **400** on the admin route). Each secret's value is encrypted with its `<orgId>:<scope>:<key>` location bound in as authenticated data, so a `:` inside the key made that binding ambiguous: a key of `c:d` in scope `b` produced the same binding as a key of `d` in scope `b:c`, and a value written at one of those locations would decrypt at the other. Accepting the character was the defect, so there is no dual-accept window. **Migration: none, and nothing already stored becomes unreachable.** Validation applies to writes only — reading, listing and deleting still accept any key — so a secret stored under a non-conforming key before the upgrade keeps resolving in workflows and can still be deleted. To rename one, write the value under a conforming key and delete the old one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v0.2.0     |
| Secret scope names (stored form)                         | A scope name is now validated segment by segment against `[A-Za-z0-9_.-]`, which admits no `:`. The `<backend>:` qualifier is no longer part of the stored name: every write plane strips it and routes the operation to that backend, so `pg:production` is stored as `production` inside the PG backend. Accepting `:` in a stored name was the defect — it made a scope reachable under one spelling and invisible under the other — so there is no dual-accept window. **Migration:** an orchestrator that predates this stored scopes with the qualifier attached, and those rows are unreachable after the upgrade. Run `kici-admin secret fix-prefixed-scopes <orgId> --database-url "$KICI_DATABASE_URL"` once per organization to rename them to their bare paths; the scope name is bound into each secret's authenticated encryption, so the command re-encrypts every value as it renames and a direct SQL rename would not work. Preview with `--dry-run`. It repairs only the `pg:` qualifier and never merges two scopes: a scope whose bare target already exists, or that carries another backend's qualifier, is reported `SKIPPED` and the command exits `2`. See [Secrets management](../operator/security/secrets.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | v0.2.0     |
| Secret scope rename (dashboard + admin API)              | Renaming a secret scope now refuses two inputs it previously accepted. A rename that **crosses backends** (`pg:a` → `vault:b`, or any pair whose qualifiers resolve to different backends) is refused with **400** and `Cannot rename a scope across backends`; it previously renamed inside the _source_ backend while reporting success, so the operator was told the secrets had moved when they had not. A rename **onto a scope that already exists** — one holding secrets, an empty-scope placeholder, or context bindings — is refused with **409** and `Secret scope '<name>' already exists`; it previously merged the two scopes, re-encrypting every non-colliding key under the destination name with no way back. The dashboard surfaces both as the operation's error text. Accepting either input was the defect, so there is no dual-accept window. **Migration:** to move a scope between backends, create the secrets in the destination backend and delete the source scope — a rename cannot do it, because the value's authenticated encryption binds the scope name and only the source store can re-encrypt. To combine two scopes, set the keys explicitly in the target.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | v0.2.0     |
| `kici compile` (impure-field `W101` warning)             | The `warning [W101]` line — printed when a pure `env` / `context` / `concurrencyGroup` function could not be inlined and fell back to the two-phase init model — is no longer emitted. Dynamic fields are now always resolved on the eval agent's init-runner, so there is no purity fallback to warn about and `kici compile` output no longer contains a `warning [W101]` line. Compilation behaviour is otherwise unchanged: dynamic fields still resolve at run time, and `kici preview` still lists the injected `__init__` job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | v0.4.0     |
| `kici compile` (approval on a global workflow)           | An `approval` gate declared on an **organization-wide** workflow — one whose trigger carries `repos:` — is now refused with `error [E124]`, at the workflow level and on any static job. The gate was never enforced there: approval holds are applied by the per-repository dispatch path, and the global path builds its job inputs and dispatches them without consulting the gate, so the job ran immediately while the author believed a human had to release it. Accepting it was the defect — a workflow claiming a security control it does not have — so there is no dual-accept window. A job produced by a `dynamicJob` generator is built on the agent and never passes through the compiler; that one is logged at `error` by the orchestrator when it dispatches, naming the workflow and job. **Migration:** drop `approval`, or move the gated jobs into a workflow whose triggers carry no `repos:`. Per-repository workflows are unaffected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | v0.5.0     |
| Role repository scope (grant ceiling)                    | A member can no longer grant a repository scope wider than the one they hold. Creating a role, updating a role, assigning a role to a member, granting a role to a team, and inviting a member into a role are each refused with **403** when the requested `repoPatterns` are not contained by the caller's own — including the case where `repoPatterns` is **omitted** on create, since the field defaults to `*`. Containment is deliberately conservative: a pattern is grantable only when the caller's scope is `*`, when the caller holds that exact pattern, or when the request names one concrete repository the caller's patterns already reach — so a genuinely narrower glob such as `myorg/backend-*` requested by a caller holding `myorg/*` is also refused, and the workaround is to grant the literal pattern. Separately, `POST /orgs/:customerId/api-keys` is refused with **403** for a repository-restricted caller, because an organization API key carries no repository scope and operates across every repository within its permission level. Widening a scope you do not hold was itself the escalation, so there is no dual-accept window. **Migration:** an organization owner (scope `*`) performs the grant, or widens the delegating member's own scope first. The break-glass `kici-platform-admin role create` is unaffected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | v0.2.0     |
| Direct webhook ingress (response semantics)              | A direct-ingress webhook (`POST /webhook/:orgId/github/:sourceId` and `POST /webhook/:orgId/generic/:sourceId`) is now answered **as soon as the delivery is durably queued**, and its match-and-dispatch pipeline runs afterwards. `202` therefore means _accepted and stored_, not _matched and dispatched_, and a pipeline failure no longer produces a **500** — the response is already sent by then. There is no dual-accept window, because a response cannot both wait for the pipeline and not wait for it. The old behaviour was itself the defect: a provider abandons a delivery attempt in seconds (GitHub's timeout is 10) while one matched workflow's build phase alone may take up to 600, so a slow build turned a delivery into a provider-side failure for work that usually succeeded. **What to do instead of reading the response:** the delivery id in the body correlates a delivery to its run (`kici-admin runs list --delivery-id`), the event log carries a per-delivery row that goes `failed` with the error message when the pipeline throws, and a failing delivery is now retried by the orchestrator itself rather than depending on the sender redelivering on a 5xx. Rejections _before_ queueing — unknown source, bad signature, oversized body, malformed headers, and a shed under load (`429` + `Retry-After`) — are unchanged and still arrive in the response. Setting `KICI_INGEST_OVERFLOW_ENABLED=false` reverts direct ingestion to waiting for the pipeline, at the cost of the durable queue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | v0.5.0     |
| Event- and schedule-triggered runs (context enforcement) | A workflow triggered by a schedule, a `kiciEvent()` emit, a workflow or job completion, or a failure batch resolves its bound contexts in full: context variables, [scoped secrets](./secrets.md), and every [protection rule](./contexts.md#protection-rules) the context carries, [concurrency groups](./concurrency.md) included. `ctx.secrets.get()` returns the secret, and a gated context gates the run. There is no dual-accept window: running such a run past a gate the operator set was itself the defect, and a grace period would hold that bypass open for its whole duration. **Migration:** bind the context on the job (`context: 'my-context'`) so the run resolves the secrets it reads. A run a context holds enters the [approval queue](./dashboard/contexts-and-secrets.md#approval-queue), which names the holding context and the reason; `kici runs show <run-id>` prints the same hold. Move a workflow that must run unattended onto an ungated context. **Branch restrictions apply to these runs too.** A schedule fire presents the repository's default branch. A `kiciEvent()` subscriber, a completion trigger and an invoke-gate summon present the branch of the run behind them. A context restricted to `main` therefore accepts a nightly deploy off `main`. A run with no single source branch is still rejected, with `Context '<name>' restricts branches: this internally-triggered run carries no branch`. Four cases present none: a failure batch (`workflowsFailedBatch`), which one window of failed runs causes on as many branches; a `kici.scaler.*` event, which the orchestrator mints with no run behind it; a schedule fire in a repository that has not pushed to its default branch since the upgrade (the default branch is captured when a push re-registers the workflows, so the next such push fixes it); and an event whose emitting run is no longer on record. See [branch restrictions](./contexts.md#branch-restrictions).                                                           | v0.6.0     |
| Event- and schedule-triggered runs (trust tier)          | An internally-triggered run resolves a [trust tier](./contexts.md#minimum-trust) from the trigger. The events the orchestrator mints with **no run behind them** — `__schedule_fire` and the two `kici.scaler.*` events — run **trusted**, so a [Dockerfile build](./container-jobs.md#who-may-build) in a cron-fired workflow builds. Every other internal trigger inherits the tier of the run that caused it: a `kiciEvent()` subscriber and the two lifecycle events a single run causes (`__workflow_complete`, `__job_complete`) inherit the emitting run's tier, `__workflows_failed_batch` resolves the most restrictive tier across the failed runs it carries, and a run summoned by an [invoke gate](./global-workflows.md#invoking-a-source-repos-own-workflows) inherits its summoner's tier. Anything unresolvable — no emitting run, an unreadable tier, a failed lookup, a batch whose carried run list was truncated or whose members cannot all be read — resolves no tier at all, which isolates the run's caches. Two consequences follow, and they have different thresholds. A subscriber that inherits any tier below `trusted` receives no install secrets, so a job that installs from a [private registry](./private-registries.md) fails at install time. A context that sets `minimumTrust` at all holds a subscriber whose tier is `unknown`, and passes every other tier — the two floors `minimumTrust` accepts block the same thing, because trust is a ref-based judgement with two answers. Both consequences are raised far from their cause, which is the tier of the **emitting** run. **Migration:** emit the event from a trusted run, or drop `minimumTrust` from the context the subscriber binds.                                                                                                                                                                                                                                                                                                              | v0.6.0     |
| Event names (`ctx.emit`)                                 | The prefix `__` is reserved for KiCI internal events, alongside the existing `kici.` reservation. `ctx.emit('__foo')` fails the step with `event name prefix "__" is reserved for KiCI internal events and cannot be emitted from a workflow step (got "__foo")`. A non-SDK caller that reaches the orchestrator gets `event name prefix "__" is reserved for KiCI internal events`, and no event row is written. The reservation is **prefix-only**: `deploy__done` still emits. Subscribing to a reserved name is unaffected — only emission is refused. There is no dual-accept window: a run triggered by `__schedule_fire` or a `kici.scaler.*` event is trusted, and every reserved name skips the event rate limiter, so a name a workflow can forge is a privilege escalation for as long as it is accepted. **Migration:** rename the event. A prefix of your own, such as `acme.`, carries no reservation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | v0.6.0     |
| `invokeSource()` (invoke-gate event names)               | An [invoke gate](./global-workflows.md#invoking-a-source-repos-own-workflows) may not name a reserved event. `invokeSource('__…')` and `invokeSource('kici.…')` fail at compile time with `invokeSource: event name prefix "__" is reserved for KiCI internal events (got "…")`. A lock file that still carries such a gate **fails the gate job** at dispatch with `invoke gate cannot summon '…': the event-name prefix "…" is reserved for KiCI internal events. Choose a name a workflow may emit.` The job's status is `failed`, not skipped, so `optional: true` does not turn the refusal green. Nothing is summoned and no proxy job is created. There is no dual-accept window, for the same reason as the emit path. **Migration:** rename the gate's event, recompile, and rename the matching `kiciEvent({ name })` in each subscribing repository.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | v0.6.0     |
| Context protection rules (rejected job)                  | A job a [context protection rule](./contexts.md#protection-rules) rejects is now recorded on the run as a **failed** job, carrying the context and the rule that rejected it. The job used to be skipped silently: it never appeared on the run, and the reason reached only the orchestrator log. **A run that completed green with the gated job absent now completes failed** — its notifications, checks and dashboards report the failure. Two related outcomes change with it: a run whose every job is gated is reported as `context_rules` with the rule's own reason, instead of "no agent available to run this job"; and a downstream job that `needs` a gated job now sees a terminal upstream, instead of waiting for a job that never arrives. Read the reason with `kici runs show <run-id>` or on the run detail page. **Migration:** a workflow that relied on a gated job disappearing must decide the skip itself — give the job a [rule](./sdk/rules-matrix-dynamic.md) that skips it on the branches or events the context refuses — or accept the failed run. There is no dual-accept window: a run reporting green while a gated deploy silently did not happen was itself the defect, and accepting it for a window would keep shipping it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | v0.6.0     |
| Fork pull requests (org policy default and scope)        | The organization's fork switch now decides everything about a pull request from a fork, and its default when no policy is stored is **`ignore`** — the event is dropped before dispatch, so there is no run row and no check run. Two behaviours tighten. An orchestrator running in **independent** mode used to enforce no policy arm at all until one was stored, so fork pull requests ran; it now applies the same `ignore` default, and `kici-admin trust-policy show` no longer reports an `Enforcement: legacy` state. And a policy read that **throws** now applies `hold` rather than the no-policy default, so a database blip parks fork pull requests visibly instead of dropping them. One behaviour relaxes: on `allow`, a fork pull request that edits `.kici/` is no longer held. The run evaluates the **base branch's** workflow definitions, so the edited ones are inert for it; the change is still reported on the neutral `KiCI: Workflow changes` check. There is no dual-accept window, because a default cannot be two values at once. **Migration:** if your organization relied on fork pull requests running, set the fork policy to `allow` (or `hold`) in **Settings > CI trust**, or with `kici-admin trust-policy set --fork-policy` on an independent orchestrator. Check it before you upgrade: an organization that has never opened that tab has no stored policy and gets `ignore`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | v0.6.0     |
| Trust tiers (ref-based resolution)                       | A run's trust tier is resolved from the **git ref** the event carries, with no provider permission call and no contributor lookup. A ref in the repository resolves `trusted`; a ref from a fork resolves `unknown`. Three consequences are worth checking. Every same-repo push now resolves `trusted`, including a topic-branch push, so such a run writes to the **organization-shared** build cache where it previously got an isolated scope. A pull request opened by a contributor with no repository access, on a same-repo branch, now runs fully trusted — pushing that branch already required write access. And a fork pull request runs untrusted whoever opened it, including an organization owner. There is no dual-accept window: a tier cannot be resolved two ways at once. The reductions an untrusted run carries are unchanged in kind — base-branch workflow definitions, no install or registry secrets, and build-cache **writes** confined to the run, while a restore still falls back to the shared scope. **Migration:** none for most organizations. If you relied on a contributor's provider permission level gating dispatch, use [branch protection](./contexts.md#branch-restrictions) on the forge and a context `minimumTrust` rule instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | v0.6.0     |
| Undispatched runs (check-run completion)                 | A run that ends **before any job starts** now completes the `kici/<workflow>` and per-job check runs its dispatch setup created. Every such path is covered — an init failure, a secret-resolution failure, a rejected or expired security hold, a workflow with no dispatchable job, and an infrastructure error that aborts the dispatch part-way. Those checks used to stay `queued` forever, which blocks branch protection with no explanation and no way to clear it short of a new commit. So a pull request whose run fails at init flips from **stuck pending** to **failing**: the status is new, the failure is not, and the check summary names the category. This reaches well beyond fork pull requests — any run with an early failure is affected. Leaving a queued check on the commit was itself the defect, so there is no dual-accept window. **Migration:** none. If a branch-protection rule required the `kici/…` check and silently never passed, it now reports the real failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | v0.6.0     |
| Security holds (terminal conclusions and summaries)      | Every terminal `KiCI Security` summary was rewritten, and `/kici reject` on a **job-scoped** hold now concludes the check `cancelled` rather than `failure`, matching the workflow-scoped path. Approving a fork-pull-request hold now **dispatches the run** — untrusted, under the same trust resolution the hold was created with — where it previously flipped the row to `approved`, posted a green check, and ran nothing. Rejection and expiry now conclude the check instead of leaving it in progress. `/kici approve` posts the terminal provider status **before** it resumes the run, so approve latency now includes one provider round-trip per hold. Check text has no old-and-new form, so none of this has a dual-accept window. **Migration:** a fork hold created **before** the upgrade and approved after it still does not dispatch — no row is migrated. Re-run those pull requests by pushing a new commit. Update any tooling that matched on the old summary text or on a `failure` conclusion for a rejected job-scoped hold.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | v0.6.0     |
| A job held by two gates                                  | A job gated by **both** a reviewer approval and a security trust hold now writes two `held_runs` rows and requires **both** to be released. The reviewer hold takes `contexts:write` plus clause eligibility; the security hold takes `ci_trust:write`. A single reviewer approval used to release such a job, so the trust requirement was not enforced — accepting that was the defect, and there is no dual-accept window. Two consequences follow. The two rows carry **independent expiries** — the reviewer row from the gate's `timeout` or the org's `approval_expiry_seconds` (default 24 hours), the `context_trust` row from the context's `hold_expiry_seconds` (default one hour) — and the earlier one cancels the run. So a job that used to wait out the reviewer deadline can now be cancelled an hour in. And `--job` alone can no longer name one of the two: `kici approve` and `kici reject` gain `--hold-type <type>` and `--hold <id>`, and the developer MCP `approve_run` / `reject_run` tools gain the matching `holdType` and `holdId` parameters. **Migration:** grant both permissions to whoever must release such a job, or split the requirement across two people. Pass `--hold-type security` or `--hold-type reviewer` in scripts that approve a job carrying both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v0.6.0     |
| `kici-admin trust-policy show` (table output)            | The human `show` table no longer prints an `Unknown contributor policy:` or `Workflow change policy:` row. Both values are deprecated and no dispatch decision reads either, so printing them asserted an enforcement that is not happening. `--format json` is unchanged and still carries both fields, so nothing machine-readable was removed. An aligned human table is not a machine contract, so there is no dual-accept window. **Migration:** a script scraping the table for either value should read `--format json` instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | v0.6.0     |
| Global-workflow policy lists (repo patterns)             | The three global-workflow repository lists — `kici-admin org-settings global-workflows allow-add` / `deny-add` / `elevate-add`, the matching orchestrator admin route, and the dashboard's Global workflows card — now refuse a pattern the matcher reads as a **negation**, and store nothing. Four forms are refused: a leading `!` (`!myorg/x`), the extglob complement (`myorg/!(secret)`), the negated character class (`myorg/[^s]*`), and the regular-expression negative assertions (`(?!myorg/legacy)**`, `**(?<!secret)`). An empty or whitespace-only pattern is refused too. A negated pattern inverts the direction the list already encodes: on the allow list `(?!myorg/legacy)**` allowed **every repository in every organization** except the one it named, and on the deny list `myorg/(?!secret)*` made `myorg/secret` the one repository the list admitted. Accepting the value was itself the defect, so there is no dual-accept window. **A pattern already stored is not applied as a negation:** the allow and elevated lists grant nothing for it, the deny list blocks, and your orchestrator logs a warning naming the organization and the pattern. The character class `myorg/[!s]*` is **not** a negation and is still accepted — the matcher reads `[!s]` as the two literal characters `!` and `s`, so the pattern matches only the repositories whose name begins with one of them. **Migration:** replace a refused entry with the repositories you mean (`allow-remove` / `deny-remove` / `elevate-remove`, then re-add).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | v0.6.0     |
| Context `minimumTrust` (off-vocabulary stored value)     | A `minimum_trust` value outside `known` / `trusted` now **holds** the run instead of passing it. Such a value can only reach storage through the admin context routes, whose schema accepts any string. Passing an unrecognised security floor was itself the defect — the safe reading of "I do not understand this bar" is to hold — so there is no dual-accept window. **Migration:** none if your contexts carry `known`, `trusted`, or no value. If a context started holding every fork run unexpectedly, read its `minimumTrust` back and set it to `trusted`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | v0.6.0     |
